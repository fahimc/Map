#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-angular-seed/bin/grunt-angular-seed" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-angular-seed/bin/grunt-angular-seed" "$@"
  ret=$?
fi
exit $ret
